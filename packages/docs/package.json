{
  "name": "@oxlint-tailwindcss/docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -C ../oxlint-tailwindcss build && pnpm generate && vitepress dev .",
    "build": "pnpm -C ../oxlint-tailwindcss build && pnpm generate && vitepress build . && node --experimental-strip-types scripts/check-dist.ts",
    "preview": "vitepress preview .",
    "generate": "node --experimental-strip-types scripts/generate-rules.ts && node --experimental-strip-types scripts/generate-blocks.ts && oxfmt --write rules es/rules setup.md es/setup.md shadcn.md es/shadcn.md migration es/migration && cd ../.. && oxfmt --write README.md packages/oxlint-tailwindcss/README.md",
    "test": "vitest run"
  },
  "devDependencies": {
    "oxlint-tailwindcss": "workspace:*",
    "vitepress": "^2.0.0-alpha.20",
    "vitest": "^5.0.1"
  }
}
