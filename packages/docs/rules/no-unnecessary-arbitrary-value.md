# no-unnecessary-arbitrary-value

> Disallow arbitrary values when a named Tailwind class produces the same CSS

## Options

| Option | Type |
|---|---|
| `entryPoint` | string |

## Default values

```json
{}
```

## Auto-fix

- This rule is **auto-fixable** (`fixable: code`).
- It also offers editor suggestions for non-primary fix candidates.

