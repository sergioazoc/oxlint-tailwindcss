{
  "name": "@oxlint-tailwindcss/docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --experimental-strip-types scripts/generate-rules.ts && vitepress dev .",
    "build": "pnpm -C ../oxlint-tailwindcss build && node --experimental-strip-types scripts/generate-rules.ts && vitepress build .",
    "preview": "vitepress preview .",
    "generate": "node --experimental-strip-types scripts/generate-rules.ts"
  },
  "devDependencies": {
    "oxlint-tailwindcss": "workspace:*",
    "vitepress": "^2.0.0-alpha.17"
  }
}
